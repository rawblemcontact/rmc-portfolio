HERO SLIDER — removed from Home.tsx <Hero> (project video/image carousel)

To restore:
1. Reintroduce heroMediaReady + preload useEffect (first-slide video/image) if gating should wait on media.
2. Re-add state: heroSlideIndex, heroSlideDirection, heroSlidePaused, heroTouchStartYRef; goToHeroSlide / interval autoplay; slider markup: motion scaleY wrapper > rounded card > AnimatePresence > article with video|img (see git history before this removal).
3. heroReady should again include heroMediaReady; heroRevealDelayDone should depend on fonts + media.
4. Phase effect: setSliderPhaseActive(true) at 1.1s after assets (not only fonts); setSliderAnimDone from onAnimationComplete on the scaleY motion.div.
5. Grid: grid-rows-[minmax(0,2fr)_minmax(0,1fr)] with top row = slider area, bottom = name + CTA; name block motion y uses sliderPhaseActive.

When SHOW_HERO_TUNER is true, slider + currentHeroSlide index are required for focal/zoom ranges.
